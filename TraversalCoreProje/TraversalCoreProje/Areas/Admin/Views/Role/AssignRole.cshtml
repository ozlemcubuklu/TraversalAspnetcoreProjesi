@model List<RoleAssingViewModel>
@{
    ViewData["Title"] = "AssignRole";
    Layout = "~/Views/Shared/_AdminLayaut.cshtml";
}

<h1>Role Ata</h1>
<form method="post">

   @for (int i = 0; i < Model.Count; i++)
    {
         <div class="form-check">
        <input type="hidden" asp-for="@Model[i].RoleId"> 
            <input type="hidden" asp-for="@Model[i].RoleName">
            <input class="form-check-input" type="checkbox" asp-for="@Model[i].RoleExist">
            <label class="form-check-label" for="@Model[i].RoleExist">
                @Model[i].RoleName
            </label>
        </div>
       
    }
@* @foreach (var item in Model)
{
    <div class="form-check">
        <input type="hidden" asp-for="@item.RoleId"> 
        <input type="hidden" asp-for="@item.RoleName"> 
       
        @if (item.RoleExist)
        {
                <input class="form-check-input" type="checkbox" asp-for="@item.RoleExist" id="@item.RoleId+@item.RoleName" checked>
        }
       else{
                <input class="form-check-input" type="checkbox" asp-for="@item.RoleExist" id="@item.RoleId+@item.RoleName">
       }
            <label class="form-check-label" for="@item.RoleId+@item.RoleName">
        @item.RoleName
    </label>
</div>

    } *@
    <input type="submit" class="btn btn-outline-primary" value="kaydet">
</form>